name: actionsPractice

on:
  workflow_dispatch:


jobs:
  my_first_job:
    env:
      LAST_NAME: Darling
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
      
      - name: Greetings
        env:
          WELL: KemCho
        run: echo "Hello, ${{ vars.FIRST_NAME}} $LAST_NAME! $WELL?"
        
      - name: List files
        run: ls -a
