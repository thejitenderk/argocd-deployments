name: actionsPractice

on:
  workflow_dispatch:

jobs:
  my_first_job:
    name: my first job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.3
      
      - name: Greetings
        run: echo "Hello World"
        
      - name: List files
        run: ls -a
